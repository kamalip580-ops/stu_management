# stu_management
**Student Management System** is a Python-based application that helps manage student records efficiently. It allows users to add, view, search, update, and delete student information through a simple menu-driven interface. The project uses JSON file handling to store data permanently, ensuring records are retained between sessions. 
